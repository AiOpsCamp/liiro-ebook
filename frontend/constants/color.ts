/**
 * ⚠️  DEPRECATED — Use `@/constants/Colors` → `brand` or `AppColors` instead.
 *
 * This file exists only for backward compatibility.
 */
import { brand } from "./Colors";

const color = {
  sunbeam: brand.sunbeam,
  "lemon-leaf": brand.lemonLeaf,
  "meadow-green": brand.meadowGreen,
  "forest-core": brand.forestCore,
};

export default color;
